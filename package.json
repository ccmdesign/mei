{
  "scripts": {
    "dev": "nuxt dev",
    "prebuild": "cd mei-fetch-data && npm run start",
    "postbuild": "cp -r assets/ .output/public/ && cp -r assets/ dist/",
    "generate": "export NODE_ENV=production && npm run prebuild && nuxt generate && npm run postbuild"
  },
  "devDependencies": {
    "@nuxt/content": "^2.2.0",
    "node-sass": "^7.0.1",
    "nuxt": "^3.0.0-rc.5"
  },
  "dependencies": {
    "@ccmdesign/ccm-ds": "^0.4.0-alpha",
    "path": "^0.12.7",
    "sass": "^1.54.4",
    "uuid": "^9.0.0"
  }
}
