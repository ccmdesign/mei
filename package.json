{
  "scripts": {
    "dev": "nuxt dev",
    "prebuild": "cd mei-fetch-data && npm run start",
    "generate": "npm run prebuild && nuxt generate"
  },
  "devDependencies": {
    "@nuxt/content": "^2.2.0",
    "node-sass": "^7.0.1",
    "nuxt": "^3.0.0-rc.5"
  },
  "dependencies": {
    "@ccmdesign/ccm-ds": "^0.2.3",
    "path": "^0.12.7",
    "sass": "^1.54.4",
    "uuid": "^9.0.0"
  }
}
